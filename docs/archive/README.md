# Archived Hackathon Patch Scripts

This directory contains throwaway scripts used during the hackathon cleanup and
demo preparation. They are retained for history only.

Do not run these scripts as part of normal development. Several were designed to
rewrite source files or mutate `node_modules` in-place, which is not appropriate
for a reusable reference app.
